# UnrailedSaveScummer
This application, when running, automatically restores the save files deleted by the game Unrailed! when loading a save game


To use the application, simply run the executable (obtainable from the Releases section). The application will notify when backups are created and restored. Do note that the application must be running for the automatic save restore to function.
